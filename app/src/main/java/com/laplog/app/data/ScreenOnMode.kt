package com.laplog.app.data

enum class ScreenOnMode {
    OFF,            // Screen can turn off normally
    WHILE_RUNNING,  // Keep screen on only while stopwatch is running
    ALWAYS          // Keep screen on always when app is open
}
